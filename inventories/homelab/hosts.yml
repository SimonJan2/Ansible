# Homelab inventory — add hosts on task branches.
#
# Example structure:
# all:
#   children:
#     proxmox:
#       hosts:
#         pve.local:
#     vms:
#       hosts:
#         docker-host.local:
