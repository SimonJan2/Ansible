# Ansible playbook to install the latest qemu-guest-agent
---
- name: Install latest qemu-guest-agent
  hosts: all
  become: true
  tasks:
    - name: Install qemu-guest-agent
      apt:
        name: qemu-guest-agent
        state: present
        update_cache: true